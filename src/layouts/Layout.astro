---
export interface Props {
	title: string;
	description?: string;
	image?: string;
}

const { title, description = "Site vitrine heroic fantasy et medieval", image = "/og-image.jpg" } = Astro.props;
---

<!doctype html>
<html lang="fr">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		
		<!-- Open Graph -->
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={image} />
		<meta property="og:type" content="website" />
		
		<!-- Twitter Card -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={image} />
		
		<!-- Google Fonts -->
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
		<style is:global>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				--font-medieval: 'Cinzel', serif;
				--font-parchment: 'Crimson Text', serif;
				--font-gothic: 'Cinzel', serif;
				/* Variables de couleurs sombres - The Witcher Style */
				--bg-dominant: #0A0A0A;
				--bg-secondary: #1A1A1A;
				--bg-tertiary: #2A2A2A;
				--accent-royal: #8B7355;
				--accent-action: #4A3C28;
				--accent-magical: #2C1810;
				--text-primary: #E8E0D5;
				--text-secondary: #B8B0A0;
				--border-gold: #6B5D54;
				--glow-gold: rgba(139, 115, 85, 0.3);
				--glow-action: rgba(74, 60, 40, 0.3);
				--glow-magical: rgba(44, 24, 16, 0.3);
			}

			html {
				scroll-behavior: smooth;
			}

			body {
				font-family: var(--font-parchment);
				background: linear-gradient(135deg, var(--bg-dominant) 0%, var(--bg-secondary) 50%, var(--bg-dominant) 100%);
				color: var(--text-primary);
				min-height: 100vh;
				position: relative;
			}

			body::before {
				content: '';
				position: fixed;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><rect width="100" height="100" fill="none" stroke="%23D4AF3710" stroke-width="0.5"/><circle cx="50" cy="50" r="20" fill="none" stroke="%23D4AF3705" stroke-width="0.3"/></svg>');
				background-size: 100px 100px;
				pointer-events: none;
				z-index: -1;
			}

			.btn-hero {
				padding: 1rem 2rem;
				background: linear-gradient(135deg, var(--accent-action), var(--accent-magical));
				color: var(--text-primary);
				font-weight: bold;
				border-radius: 0.25rem;
				border: 2px solid var(--accent-royal);
				box-shadow: 0 4px 15px rgba(114, 47, 55, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.1);
				transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
				font-family: var(--font-gothic);
				cursor: pointer;
				text-decoration: none;
				display: inline-block;
				position: relative;
				overflow: hidden;
				text-transform: uppercase;
				letter-spacing: 1px;
				font-size: 0.9rem;
			}

			.btn-hero::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
				transition: left 0.6s ease;
			}

			.btn-hero:hover::before {
				left: 100%;
			}

			.btn-hero:hover {
				transform: translateY(-3px) scale(1.02);
				box-shadow: 0 8px 25px rgba(114, 47, 55, 0.6), inset 0 1px 0 rgba(255, 255, 255, 0.2);
				border-color: var(--accent-royal);
				background: linear-gradient(135deg, var(--accent-magical), var(--accent-action));
			}

			.btn-secondary {
				padding: 1rem 2rem;
				background: linear-gradient(135deg, var(--bg-secondary), var(--bg-dominant));
				color: var(--accent-royal);
				font-weight: bold;
				border-radius: 0.25rem;
				border: 2px solid var(--accent-royal);
				box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(212, 175, 55, 0.1);
				transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
				font-family: var(--font-gothic);
				cursor: pointer;
				text-decoration: none;
				display: inline-block;
				position: relative;
				overflow: hidden;
				text-transform: uppercase;
				letter-spacing: 1px;
				font-size: 0.9rem;
			}

			.btn-secondary::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.2), transparent);
				transition: left 0.6s ease;
			}

			.btn-secondary:hover::before {
				left: 100%;
			}

			.btn-secondary:hover {
				transform: translateY(-3px) scale(1.02);
				box-shadow: 0 8px 25px rgba(212, 175, 55, 0.3), inset 0 1px 0 rgba(212, 175, 55, 0.2);
				background: linear-gradient(135deg, var(--bg-dominant), var(--bg-secondary));
				color: var(--text-primary);
			}

			.card-medieval {
				background: linear-gradient(135deg, rgba(44, 36, 30, 0.95), rgba(26, 26, 29, 0.95));
				backdrop-filter: blur(10px);
				border: 2px solid var(--border-gold);
				border-radius: 0.5rem;
				padding: 2rem;
				box-shadow: 0 15px 35px rgba(0, 0, 0, 0.6), inset 0 1px 0 rgba(212, 175, 55, 0.1);
				position: relative;
				overflow: hidden;
			}

			.card-medieval::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 2px;
				background: linear-gradient(90deg, transparent, var(--accent-royal), transparent);
			}

			.card-medieval::after {
				content: '';
				position: absolute;
				bottom: 0;
				left: 0;
				right: 0;
				height: 2px;
				background: linear-gradient(90deg, transparent, var(--accent-royal), transparent);
			}

			.text-hero {
				font-family: var(--font-gothic);
				font-size: 3.5rem;
				font-weight: 900;
				background: linear-gradient(135deg, var(--accent-royal), var(--accent-action));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				color: transparent;
				text-shadow: 0 0 30px var(--glow-gold);
				position: relative;
				text-transform: uppercase;
				letter-spacing: 2px;
				line-height: 1.1;
			}

			.text-hero::after {
				content: attr(data-text);
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(135deg, var(--accent-royal), var(--accent-action));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				z-index: -1;
				filter: blur(10px);
				opacity: 0.4;
			}

			@media (min-width: 768px) {
				.text-hero {
					font-size: 5rem;
				}
			}

			.text-title {
				font-family: var(--font-gothic);
				font-size: 2rem;
				font-weight: 700;
				color: var(--accent-royal);
				text-shadow: 0 0 20px var(--glow-gold);
				text-transform: uppercase;
				letter-spacing: 1px;
				position: relative;
			}

			.text-title::after {
				content: '';
				position: absolute;
				bottom: -10px;
				left: 50%;
				transform: translateX(-50%);
				width: 100px;
				height: 2px;
				background: linear-gradient(90deg, transparent, var(--accent-royal), transparent);
			}

			@media (min-width: 768px) {
				.text-title {
					font-size: 2.5rem;
				}
			}

			.border-medieval {
				border: 2px solid var(--border-gold);
				border-radius: 0.5rem;
				position: relative;
			}

			.border-medieval::before {
				content: '';
				position: absolute;
				top: -1px;
				left: -1px;
				right: -1px;
				bottom: -1px;
				background: linear-gradient(45deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				border-radius: 0.5rem;
				z-index: -1;
				opacity: 0.3;
			}

			.glow-effect {
				box-shadow: 0 0 40px var(--glow-gold);
			}

			.text-shadow {
				text-shadow: 2px 2px 8px var(--shadow-dark);
			}

			.text-shadow-lg {
				text-shadow: 4px 4px 16px var(--shadow-dark);
			}

			.backdrop-medieval {
				backdrop-filter: blur(15px) brightness(0.6);
			}

			/* Séparateurs décoratifs */
			.divider-celtic {
				height: 3px;
				background: linear-gradient(90deg, transparent, var(--accent-royal), transparent);
				position: relative;
				margin: 2rem 0;
			}

			.divider-celtic::before,
			.divider-celtic::after {
				content: '⚔';
				position: absolute;
				top: 50%;
				transform: translateY(-50%);
				color: var(--accent-royal);
				font-size: 1.2rem;
			}

			.divider-celtic::before {
				left: 45%;
			}

			.divider-celtic::after {
				right: 45%;
			}

			/* Classes utilitaires de base */
			.container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 1rem;
			}

			.flex {
				display: flex;
			}

			.items-center {
				align-items: center;
			}

			.justify-center {
				justify-content: center;
			}

			.justify-between {
				justify-content: space-between;
			}

			.space-x-2 > * + * {
				margin-left: 0.5rem;
			}

			.space-x-4 > * + * {
				margin-left: 1rem;
			}

			.space-x-8 > * + * {
				margin-left: 2rem;
			}

			.space-y-2 > * + * {
				margin-top: 0.5rem;
			}

			.space-y-4 > * + * {
				margin-top: 1rem;
			}

			.space-y-6 > * + * {
				margin-top: 1.5rem;
			}

			.gap-4 {
				gap: 1rem;
			}

			.gap-6 {
				gap: 1.5rem;
			}

			.gap-8 {
				gap: 2rem;
			}

			.gap-12 {
				gap: 3rem;
			}

			.grid {
				display: grid;
			}

			.grid-cols-1 {
				grid-template-columns: repeat(1, minmax(0, 1fr));
			}

			.grid-cols-2 {
				grid-template-columns: repeat(2, minmax(0, 1fr));
			}

			.grid-cols-3 {
				grid-template-columns: repeat(3, minmax(0, 1fr));
			}

			@media (min-width: 768px) {
				.md\:grid-cols-2 {
					grid-template-columns: repeat(2, minmax(0, 1fr));
				}

				.md\:grid-cols-3 {
					grid-template-columns: repeat(3, minmax(0, 1fr));
				}

				.md\:flex {
					display: flex;
				}

				.md\:text-2xl {
					font-size: 1.5rem;
				}

				.md\:text-4xl {
					font-size: 2.25rem;
				}

				.md\:text-6xl {
					font-size: 3.75rem;
				}
			}

			@media (min-width: 1024px) {
				.lg\:grid-cols-2 {
					grid-template-columns: repeat(2, minmax(0, 1fr));
				}

				.lg\:grid-cols-3 {
					grid-template-columns: repeat(3, minmax(0, 1fr));
				}
			}

			.text-center {
				text-align: center;
			}

			.text-white {
				color: white;
			}

			.text-gold {
				color: #FFD700;
			}

			.text-parchment {
				color: #F4E8D0;
			}

			.text-parchment\/80 {
				color: rgba(244, 232, 208, 0.8);
			}

			.text-parchment\/60 {
				color: rgba(244, 232, 208, 0.6);
			}

			.text-parchment\/40 {
				color: rgba(244, 232, 208, 0.4);
			}

			.bg-night {
				background-color: #0F172A;
			}

			.bg-obsidian {
				background-color: #1a1a1a;
			}

			.bg-obsidian\/80 {
				background-color: rgba(26, 26, 26, 0.8);
			}

			.bg-dragon-red {
				background-color: #8B0000;
			}

			.bg-dragon-red\/10 {
				background-color: rgba(139, 0, 0, 0.1);
			}

			.bg-fire {
				background-color: #FF6B35;
			}

			.bg-fire\/10 {
				background-color: rgba(255, 107, 53, 0.1);
			}

			.bg-forest {
				background-color: #228B22;
			}

			.bg-stone {
				background-color: #708090;
			}

			.bg-royal {
				background-color: #4B0082;
			}

			.bg-ice {
				background-color: #E0FFFF;
			}

			.bg-gold {
				background-color: #FFD700;
			}

			.bg-gold\/10 {
				background-color: rgba(255, 215, 0, 0.1);
			}

			.bg-bronze {
				background-color: #CD7F32;
			}

			.bg-bronze\/30 {
				background-color: rgba(205, 127, 50, 0.3);
			}

			.bg-medieval-800\/50 {
				background-color: rgba(52, 58, 32, 0.5);
			}

			.bg-gradient-to-br {
				background-image: linear-gradient(to bottom right, var(--tw-gradient-stops));
			}

			.bg-gradient-to-r {
				background-image: linear-gradient(to right, var(--tw-gradient-stops));
			}

			.bg-gradient-to-t {
				background-image: linear-gradient(to top, var(--tw-gradient-stops));
			}

			.bg-gradient-to-b {
				background-image: linear-gradient(to bottom, var(--tw-gradient-stops));
			}

			.from-night {
				--tw-gradient-from: #0F172A;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(15 23 42 / 0));
			}

			.from-obsidian {
				--tw-gradient-from: #1a1a1a;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(26 26 26 / 0));
			}

			.from-dragon-red {
				--tw-gradient-from: #8B0000;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(139 0 0 / 0));
			}

			.from-fire {
				--tw-gradient-from: #FF6B35;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(255 107 53 / 0));
			}

			.from-forest {
				--tw-gradient-from: #228B22;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(34 139 34 / 0));
			}

			.from-stone {
				--tw-gradient-from: #708090;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(112 128 144 / 0));
			}

			.from-royal {
				--tw-gradient-from: #4B0082;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(75 0 130 / 0));
			}

			.from-bronze {
				--tw-gradient-from: #CD7F32;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(205 127 50 / 0));
			}

			.from-gold {
				--tw-gradient-from: #FFD700;
				--tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to, rgb(255 215 0 / 0));
			}

			.to-fire {
				--tw-gradient-to: #FF6B35;
			}

			.to-dragon-red {
				--tw-gradient-to: #8B0000;
			}

			.to-stone {
				--tw-gradient-to: #708090;
			}

			.to-ice {
				--tw-gradient-to: #E0FFFF;
			}

			.to-gold {
				--tw-gradient-to: #FFD700;
			}

			.to-obsidian {
				--tw-gradient-to: #1a1a1a;
			}

			.to-transparent {
				--tw-gradient-to: transparent;
			}

			.border-bronze\/30 {
				border-color: rgba(205, 127, 50, 0.3);
			}

			.border-bronze\/50 {
				border-color: rgba(205, 127, 50, 0.5);
			}

			.border-b {
				border-bottom-width: 1px;
			}

			.border {
				border-width: 1px;
				border-style: solid;
			}

			.rounded-lg {
				border-radius: 0.5rem;
			}

			.rounded-full {
				border-radius: 50%;
			}

			.p-4 {
				padding: 1rem;
			}

			.p-6 {
				padding: 1.5rem;
			}

			.p-8 {
				padding: 2rem;
			}

			.px-4 {
				padding-left: 1rem;
				padding-right: 1rem;
			}

			.py-3 {
				padding-top: 0.75rem;
				padding-bottom: 0.75rem;
			}

			.py-4 {
				padding-top: 1rem;
				padding-bottom: 1rem;
			}

			.py-20 {
				padding-top: 5rem;
				padding-bottom: 5rem;
			}

			.py-12 {
				padding-top: 3rem;
				padding-bottom: 3rem;
			}

			.px-4 {
				padding-left: 1rem;
				padding-right: 1rem;
			}

			.mt-4 {
				margin-top: 1rem;
			}

			.mt-8 {
				margin-top: 2rem;
			}

			.mt-16 {
				margin-top: 4rem;
			}

			.mb-2 {
				margin-bottom: 0.5rem;
			}

			.mb-3 {
				margin-bottom: 0.75rem;
			}

			.mb-4 {
				margin-bottom: 1rem;
			}

			.mb-6 {
				margin-bottom: 1.5rem;
			}

			.mb-8 {
				margin-bottom: 2rem;
			}

			.mb-16 {
				margin-bottom: 4rem;
			}

			.w-10 {
				width: 2.5rem;
			}

			.w-16 {
				width: 4rem;
			}

			.w-24 {
				width: 6rem;
			}

			.w-32 {
				width: 8rem;
			}

			.w-48 {
				width: 12rem;
			}

			.w-64 {
				width: 16rem;
			}

			.w-full {
				width: 100%;
			}

			.h-10 {
				height: 2.5rem;
			}

			.h-16 {
				height: 4rem;
			}

			.h-24 {
				height: 6rem;
			}

			.h-32 {
				height: 8rem;
			}

			.h-48 {
				height: 12rem;
			}

			.h-64 {
				height: 16rem;
			}

			.h-screen {
				height: 100vh;
			}

			.min-h-screen {
				min-height: 100vh;
			}

			.max-w-6xl {
				max-width: 72rem;
			}

			.max-w-4xl {
				max-width: 56rem;
			}

			.max-w-3xl {
				max-width: 48rem;
			}

			.mx-auto {
				margin-left: auto;
				margin-right: auto;
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.transition-all {
				transition-property: all;
				transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
				transition-duration: 150ms;
			}

			.duration-300 {
				transition-duration: 300ms;
			}

			.duration-500 {
				transition-duration: 500ms;
			}

			.hover\:scale-105:hover {
				--tw-scale-x: 1.05;
				--tw-scale-y: 1.05;
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.backdrop-blur-md {
				backdrop-filter: blur(12px);
			}

			.backdrop-blur-sm {
				backdrop-filter: blur(4px);
			}

			.fixed {
				position: fixed;
			}

			.relative {
				position: relative;
			}

			.absolute {
				position: absolute;
			}

			.top-0 {
				top: 0px;
			}

			.bottom-8 {
				bottom: 2rem;
			}

			.left-10 {
				left: 2.5rem;
			}

			.left-1\/4 {
				left: 25%;
			}

			.right-20 {
				right: 5rem;
			}

			.top-20 {
				top: 5rem;
			}

			.top-40 {
				top: 10rem;
			}

			.bottom-20 {
				bottom: 5rem;
			}

			.z-10 {
				z-index: 10;
			}

			.z-50 {
				z-index: 50;
			}

			.inset-0 {
				top: 0px;
				right: 0px;
				bottom: 0px;
				left: 0px;
			}

			.overflow-hidden {
				overflow: hidden;
			}

			.rounded-lg {
				border-radius: 0.5rem;
			}

			.blur-3xl {
				--tw-blur: blur(64px);
				filter: var(--tw-blur) var(--tw-brightness) var(--tw-contrast) var(--tw-grayscale) var(--tw-hue-rotate) var(--tw-invert) var(--tw-saturate) var(--tw-sepia) var(--tw-drop-shadow);
			}

			.text-xl {
				font-size: 1.25rem;
				line-height: 1.75rem;
			}

			.text-2xl {
				font-size: 1.5rem;
				line-height: 2rem;
			}

			.text-3xl {
				font-size: 1.875rem;
				line-height: 2.25rem;
			}

			.font-bold {
				font-weight: 700;
			}

			.font-semibold {
				font-weight: 600;
			}

			.text-sm {
				font-size: 0.875rem;
				line-height: 1.25rem;
			}

			.leading-relaxed {
				line-height: 1.625;
			}

			.opacity-90 {
				opacity: 0.9;
			}

			.animate-float {
				animation: float 3s ease-in-out infinite;
			}

			.animate-glow {
				animation: glow 2s ease-in-out infinite alternate;
			}

			.animate-fade-in {
				animation: fade-in 1s ease-out forwards;
			}

			.animate-bounce {
				animation: bounce 1s infinite;
			}

			.animate-pulse {
				animation: pulse 2s ease-in-out infinite;
			}

			.animate-shimmer {
				background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
				background-size: 1000px 100%;
				animation: shimmer 3s infinite;
			}

			.animate-sparkle {
				animation: sparkle 2s ease-in-out infinite;
			}

			.animate-magical-particles {
				animation: magical-particles 4s ease-out infinite;
			}

			.animate-text-glow {
				animation: text-glow 2s ease-in-out infinite alternate;
			}

			.animate-border-flow {
				animation: border-flow 4s linear infinite;
			}

			.animate-mystical-rotate {
				animation: mystical-rotate 8s linear infinite;
			}

			.animate-ethereal-float {
				animation: ethereal-float 10s ease-in-out infinite;
			}

			.animate-dragon-breath {
				animation: dragon-breath 5s ease-in-out infinite;
			}

			/* Animations magiques avancées */
			@keyframes float {
				0%, 100% { 
					transform: translateY(0px) rotate(0deg); 
				}
				25% { 
					transform: translateY(-8px) rotate(2deg); 
				}
				50% { 
					transform: translateY(-12px) rotate(-1deg); 
				}
				75% { 
					transform: translateY(-6px) rotate(1deg); 
				}
			}

			@keyframes glow {
				0%, 100% { 
					box-shadow: 0 0 8px var(--glow-gold), 0 0 16px var(--glow-gold), 0 0 24px var(--glow-gold); 
					filter: brightness(1.2);
				}
				50% { 
					box-shadow: 0 0 12px var(--glow-royal), 0 0 24px var(--glow-royal), 0 0 36px var(--glow-royal); 
					filter: brightness(1.4);
				}
			}

			@keyframes fade-in {
				from { 
					opacity: 0; 
					transform: translateY(30px) scale(0.95) rotateX(10deg); 
				}
				to { 
					opacity: 1; 
					transform: translateY(0) scale(1) rotateX(0deg); 
				}
			}

			@keyframes bounce {
				0%, 100% { 
					transform: translateY(-15%) scaleY(1.05) scaleX(0.95); 
					animation-timing-function: cubic-bezier(0.8,0,1,1); 
				}
				50% { 
					transform: translateY(0) scaleY(1) scaleX(1); 
					animation-timing-function: cubic-bezier(0,0,0.2,1); 
				}
			}

			@keyframes magical-particles {
				0% { 
					transform: translateY(0) translateX(0) rotate(0deg); 
					opacity: 0; 
				}
				20% { 
					opacity: 1; 
				}
				100% { 
					transform: translateY(-60px) translateX(20px) rotate(360deg); 
					opacity: 0; 
				}
			}

			@keyframes text-glow {
				0%, 100% { 
					text-shadow: 0 0 15px var(--glow-gold), 0 0 30px var(--glow-gold), 0 0 45px var(--glow-gold); 
				}
				50% { 
					text-shadow: 0 0 20px var(--glow-royal), 0 0 40px var(--glow-royal), 0 0 60px var(--glow-royal); 
				}
			}

			@keyframes border-flow {
				0% { 
					border-color: var(--accent-royal); 
					box-shadow: 0 0 8px var(--accent-royal); 
				}
				25% { 
					border-color: var(--accent-action); 
					box-shadow: 0 0 8px var(--accent-action); 
				}
				50% { 
					border-color: var(--accent-magical); 
					box-shadow: 0 0 8px var(--accent-magical); 
				}
				75% { 
					border-color: var(--accent-royal); 
					box-shadow: 0 0 8px var(--accent-royal); 
				}
				100% { 
					border-color: var(--accent-royal); 
					box-shadow: 0 0 8px var(--accent-royal); 
				}
			}

			/* Nouvelles animations Fantasy Heroïques */
			@keyframes sword-swing {
				0%, 100% { 
					transform: rotate(-10deg) translateY(0); 
				}
				50% { 
					transform: rotate(10deg) translateY(-5px); 
				}
			}

			@keyframes shield-bash {
				0%, 100% { 
					transform: scale(1) rotate(0deg); 
				}
				25% { 
					transform: scale(0.95) rotate(-5deg); 
				}
				75% { 
					transform: scale(1.05) rotate(5deg); 
				}
			}

			@keyframes fireball-flicker {
				0%, 100% { 
					opacity: 0.8; 
					transform: scale(1) rotate(0deg); 
					filter: hue-rotate(0deg) brightness(1.2);
				}
				25% { 
					opacity: 1; 
					transform: scale(1.1) rotate(90deg); 
					filter: hue-rotate(20deg) brightness(1.4);
				}
				50% { 
					opacity: 0.9; 
					transform: scale(0.95) rotate(180deg); 
					filter: hue-rotate(-20deg) brightness(1.1);
				}
				75% { 
					opacity: 1; 
					transform: scale(1.05) rotate(270deg); 
					filter: hue-rotate(10deg) brightness(1.3);
				}
			}

			@keyframes dragon-roar {
				0%, 100% { 
					transform: scale(1) rotate(0deg); 
					filter: drop-shadow(0 0 10px var(--accent-action));
				}
				25% { 
					transform: scale(1.1) rotate(-2deg); 
					filter: drop-shadow(0 0 20px var(--accent-royal));
				}
				50% { 
					transform: scale(1.15) rotate(2deg); 
					filter: drop-shadow(0 0 30px var(--accent-magical));
				}
				75% { 
					transform: scale(1.05) rotate(-1deg); 
					filter: drop-shadow(0 0 15px var(--accent-action));
				}
			}

			@keyframes crown-shine {
				0%, 100% { 
					filter: brightness(1) drop-shadow(0 0 5px var(--accent-royal)); 
					transform: translateY(0) rotate(0deg);
				}
				50% { 
					filter: brightness(1.5) drop-shadow(0 0 20px var(--accent-royal)); 
					transform: translateY(-3px) rotate(5deg);
				}
			}

			@keyframes scroll-unroll {
				0% { 
					transform: rotateX(90deg) scale(0.8); 
					opacity: 0; 
				}
				50% { 
					transform: rotateX(45deg) scale(1.1); 
					opacity: 0.8; 
				}
				100% { 
					transform: rotateX(0deg) scale(1); 
					opacity: 1; 
				}
			}

			@keyframes crystal-resonate {
				0%, 100% { 
					transform: scale(1) rotate(0deg); 
					filter: brightness(1) hue-rotate(0deg);
				}
				25% { 
					transform: scale(1.1) rotate(90deg); 
					filter: brightness(1.3) hue-rotate(60deg);
				}
				50% { 
					transform: scale(0.9) rotate(180deg); 
					filter: brightness(1.5) hue-rotate(120deg);
				}
				75% { 
					transform: scale(1.05) rotate(270deg); 
					filter: brightness(1.2) hue-rotate(180deg);
				}
			}

			@keyframes mystical-rotate {
				0% { 
					transform: rotate(0deg) scale(1); 
				}
				100% { 
					transform: rotate(360deg) scale(1); 
				}
			}

			@keyframes ethereal-float {
				0%, 100% { 
					transform: translateY(0px) translateX(0px) rotate(0deg); 
					filter: hue-rotate(0deg); 
				}
				25% { 
					transform: translateY(-15px) translateX(10px) rotate(90deg); 
					filter: hue-rotate(30deg); 
				}
				50% { 
					transform: translateY(-25px) translateX(-10px) rotate(180deg); 
					filter: hue-rotate(60deg); 
				}
				75% { 
					transform: translateY(-15px) translateX(5px) rotate(270deg); 
					filter: hue-rotate(30deg); 
				}
			}

			@keyframes dragon-breath {
				0%, 100% { 
					background: linear-gradient(45deg, var(--accent-action), var(--accent-magical), var(--accent-royal)); 
					background-size: 200% 200%; 
					filter: brightness(1);
				}
				50% { 
					background: linear-gradient(225deg, var(--accent-royal), var(--accent-action), var(--accent-magical)); 
					background-size: 300% 300%; 
					filter: brightness(1.3);
				}
			}

			/* Classes d'animations magiques */
			.animate-float {
				animation: float 6s ease-in-out infinite;
			}

			.animate-glow {
				animation: glow 3s ease-in-out infinite alternate;
			}

			.animate-fade-in {
				animation: fade-in 1s ease-out forwards;
			}

			.animate-bounce {
				animation: bounce 1s infinite;
			}

			.animate-pulse {
				animation: pulse 2s ease-in-out infinite;
			}

			.animate-shimmer {
				background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
				background-size: 1000px 100%;
				animation: shimmer 3s infinite;
			}

			.animate-sparkle {
				animation: sparkle 2s ease-in-out infinite;
			}

			.animate-magical-particles {
				animation: magical-particles 4s ease-out infinite;
			}

			.animate-text-glow {
				animation: text-glow 2s ease-in-out infinite alternate;
			}

			.animate-border-flow {
				animation: border-flow 4s linear infinite;
			}

			.animate-mystical-rotate {
				animation: mystical-rotate 8s linear infinite;
			}

			.animate-ethereal-float {
				animation: ethereal-float 10s ease-in-out infinite;
			}

			.animate-dragon-breath {
				animation: dragon-breath 5s ease-in-out infinite;
			}

			/* Classes d'animations Fantasy Heroïques */
			.animate-sword-swing {
				animation: sword-swing 2s ease-in-out infinite;
			}

			.animate-shield-bash {
				animation: shield-bash 3s ease-in-out infinite;
			}

			.animate-fireball-flicker {
				animation: fireball-flicker 1.5s ease-in-out infinite;
			}

			.animate-dragon-roar {
				animation: dragon-roar 4s ease-in-out infinite;
			}

			.animate-crown-shine {
				animation: crown-shine 3s ease-in-out infinite;
			}

			.animate-scroll-unroll {
				animation: scroll-unroll 2s ease-out forwards;
			}

			.animate-crystal-resonate {
				animation: crystal-resonate 6s ease-in-out infinite;
			}

			.animate-fade-in {
				animation: fade-in 1.2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
			}

			.animate-bounce {
				animation: bounce 2s infinite;
			}

			.animate-pulse {
				animation: pulse 2s ease-in-out infinite;
			}

			.animate-shimmer {
				background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
				background-size: 1000px 100%;
				animation: shimmer 3s infinite;
			}

			.animate-sparkle {
				animation: sparkle 2s ease-in-out infinite;
			}

			.animate-magical-particles {
				animation: magical-particles 4s ease-out infinite;
			}

			.animate-text-glow {
				animation: text-glow 2s ease-in-out infinite alternate;
			}

			.animate-border-flow {
				animation: border-flow 4s linear infinite;
			}

			.animate-mystical-rotate {
				animation: mystical-rotate 8s linear infinite;
			}

			.animate-ethereal-float {
				animation: ethereal-float 10s ease-in-out infinite;
			}

			.animate-dragon-breath {
				animation: dragon-breath 5s ease-in-out infinite;
			}

			/* Effets de particules magiques */
			.magical-particles::before {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				width: 4px;
				height: 4px;
				background: var(--accent-royal);
				border-radius: 50%;
				box-shadow: 
					0 0 10px var(--accent-royal),
					20px 20px 0 var(--accent-action),
					-20px 20px 0 var(--accent-magical),
					20px -20px 0 var(--accent-royal),
					-20px -20px 0 var(--accent-action);
				animation: magical-particles 3s ease-out infinite;
				pointer-events: none;
			}

			/* Effet de brillance magique */
			.magical-shimmer {
				position: relative;
				overflow: hidden;
			}

			.magical-shimmer::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(
					90deg,
					transparent,
					rgba(212, 175, 55, 0.4),
					rgba(114, 47, 55, 0.3),
					rgba(75, 0, 130, 0.4),
					transparent
				);
				animation: shimmer 4s infinite;
				pointer-events: none;
			}

			/* Effet de texte ancien */
			.ancient-text {
				position: relative;
				text-shadow: 
					0 0 5px var(--glow-gold),
					1px 1px 0 var(--accent-action),
					2px 2px 0 var(--accent-magical),
					3px 3px 0 var(--accent-royal);
			}

			/* Effet de bordure magique */
			.magical-border {
				position: relative;
				border: 2px solid transparent;
				background: linear-gradient(var(--bg-dominant), var(--bg-dominant)) padding-box,
					linear-gradient(45deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal)) border-box;
				animation: border-flow 3s linear infinite;
			}

			/* Effet de carte mystique */
			.mystical-card {
				position: relative;
				overflow: hidden;
			}

			.mystical-card::before {
				content: '';
				position: absolute;
				top: -2px;
				left: -2px;
				right: -2px;
				bottom: -2px;
				background: linear-gradient(45deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				border-radius: inherit;
				z-index: -1;
				animation: border-flow 4s linear infinite;
			}

			.mystical-card::after {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: linear-gradient(var(--bg-secondary), var(--bg-dominant));
				border-radius: calc(0.5rem - 2px);
				z-index: -1;
			}

			/* Effet de bouton magique */
			.magical-button {
				position: relative;
				overflow: hidden;
				transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}

			.magical-button::before {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				width: 0;
				height: 0;
				background: radial-gradient(circle, rgba(212, 175, 55, 0.6), transparent);
				transition: all 0.6s ease;
				transform: translate(-50%, -50%);
			}

			.magical-button:hover::before {
				width: 300px;
				height: 300px;
			}

			/* Icônes SVG Heroic Fantasy */
			.icon-sword {
				width: 24px;
				height: 24px;
				fill: var(--accent-royal);
				filter: drop-shadow(0 0 5px var(--glow-gold));
				animation: mystical-rotate 4s linear infinite;
			}

			.icon-shield {
				width: 24px;
				height: 24px;
				fill: var(--accent-action);
				filter: drop-shadow(0 0 5px var(--glow-action));
				animation: pulse 2s ease-in-out infinite;
			}

			.icon-fireball {
				width: 24px;
				height: 24px;
				fill: var(--accent-magical);
				filter: drop-shadow(0 0 8px var(--glow-action));
				animation: ethereal-float 3s ease-in-out infinite;
			}

			.icon-dragon {
				width: 32px;
				height: 32px;
				fill: var(--accent-royal);
				filter: drop-shadow(0 0 10px var(--glow-gold));
				animation: dragon-breath 5s ease-in-out infinite;
			}

			.icon-crown {
				width: 24px;
				height: 24px;
				fill: var(--accent-royal);
				filter: drop-shadow(0 0 6px var(--glow-gold));
				animation: glow 2s ease-in-out infinite alternate;
			}

			.icon-scroll {
				width: 20px;
				height: 20px;
				fill: var(--text-primary);
				filter: drop-shadow(0 0 3px var(--glow-gold));
			}

			.icon-crystal {
				width: 20px;
				height: 20px;
				fill: var(--accent-magical);
				filter: drop-shadow(0 0 8px var(--glow-action));
				animation: sparkle 1.5s ease-in-out infinite;
			}

			/* Effets de particules améliorés */
			.particles-container {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				pointer-events: none;
				z-index: 1;
				overflow: hidden;
			}

			.particle {
				position: absolute;
				font-size: 16px;
				opacity: 0.7;
				animation: float 8s ease-in-out infinite;
			}

			.particle.sword { animation-delay: 0s; color: var(--accent-royal); }
			.particle.shield { animation-delay: 2s; color: var(--accent-action); }
			.particle.fire { animation-delay: 4s; color: var(--accent-magical); }
			.particle.dragon { animation-delay: 6s; color: var(--accent-royal); }

			/* Background épique avec motifs */
			body::before {
				content: '';
				position: fixed;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: 
					url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="200" height="200" viewBox="0 0 200 200"><g fill="none" stroke="%23D4AF3708" stroke-width="1"><path d="M20,20 Q100,40 180,20 Q160,100 180,180 Q100,160 20,180 Q40,100 20,20"/><circle cx="100" cy="100" r="60" stroke="%23722F3705"/><path d="M50,50 L150,150 M150,50 L50,150" stroke="%234B008203"/></g></svg>'),
					url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><text x="50" y="50" font-family="serif" font-size="20" fill="%23D4AF3703" text-anchor="middle" dominant-baseline="middle">⚔</text></svg>');
				background-size: 200px 200px, 100px 100px;
				background-position: 0 0, 50px 50px;
				pointer-events: none;
				z-index: -1;
				animation: background-shift 20s linear infinite;
			}

			@keyframes background-shift {
				0% { background-position: 0 0, 50px 50px; }
				100% { background-position: 200px 200px, 150px 150px; }
			}

			/* Cartes améliorées avec effets 3D */
			.card-epic {
				background: linear-gradient(135deg, rgba(44, 36, 30, 0.95), rgba(26, 26, 29, 0.95));
				backdrop-filter: blur(15px);
				border: 3px solid transparent;
				border-radius: 1rem;
				padding: 2.5rem;
				box-shadow: 
					0 20px 40px rgba(0, 0, 0, 0.7),
					inset 0 1px 0 rgba(212, 175, 55, 0.2),
					inset 0 -1px 0 rgba(114, 47, 55, 0.3);
				position: relative;
				overflow: hidden;
				transform-style: preserve-3d;
				transition: transform 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}

			.card-epic::before {
				content: '';
				position: absolute;
				top: -3px;
				left: -3px;
				right: -3px;
				bottom: -3px;
				background: linear-gradient(45deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				border-radius: 1rem;
				z-index: -1;
				animation: border-flow 4s linear infinite;
			}

			.card-epic::after {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: 
					linear-gradient(135deg, rgba(212, 175, 55, 0.1) 0%, transparent 30%, transparent 70%, rgba(212, 175, 55, 0.1) 100%),
					linear-gradient(var(--bg-secondary), var(--bg-dominant));
				border-radius: calc(1rem - 3px);
				z-index: -1;
			}

			.card-epic:hover {
				transform: translateY(-10px) rotateX(5deg);
				box-shadow: 
					0 30px 60px rgba(0, 0, 0, 0.8),
					inset 0 1px 0 rgba(212, 175, 55, 0.3),
					inset 0 -1px 0 rgba(114, 47, 55, 0.4);
			}

			/* Boutons épiques avec effets avancés */
			.btn-epic {
				padding: 1.2rem 2.5rem;
				background: linear-gradient(135deg, var(--accent-action), var(--accent-magical), var(--accent-royal));
				background-size: 300% 300%;
				color: var(--text-primary);
				font-weight: 900;
				border-radius: 0.5rem;
				border: 3px solid var(--accent-royal);
				box-shadow: 
					0 8px 25px rgba(114, 47, 55, 0.6),
					inset 0 2px 0 rgba(255, 255, 255, 0.2),
					inset 0 -2px 0 rgba(0, 0, 0, 0.3);
				transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
				font-family: var(--font-gothic);
				cursor: pointer;
				text-decoration: none;
				display: inline-flex;
				align-items: center;
				gap: 0.8rem;
				position: relative;
				overflow: hidden;
				text-transform: uppercase;
				letter-spacing: 2px;
				font-size: 1rem;
				background-position: 0% 50%;
			}

			.btn-epic::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
				transition: left 0.8s ease;
			}

			.btn-epic::after {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				width: 0;
				height: 0;
				background: radial-gradient(circle, rgba(212, 175, 55, 0.8), transparent);
				transition: all 0.8s ease;
				transform: translate(-50%, -50%);
			}

			.btn-epic:hover {
				transform: translateY(-5px) scale(1.05);
				box-shadow: 
					0 15px 40px rgba(114, 47, 55, 0.8),
					inset 0 2px 0 rgba(255, 255, 255, 0.3),
					inset 0 -2px 0 rgba(0, 0, 0, 0.4);
				background-position: 100% 50%;
				border-color: var(--text-primary);
			}

			.btn-epic:hover::before {
				left: 100%;
			}

			.btn-epic:hover::after {
				width: 400px;
				height: 400px;
			}

			/* Titres épiques avec effets 3D */
			.title-epic {
				font-family: var(--font-gothic);
				font-size: 4rem;
				font-weight: 900;
				background: linear-gradient(135deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				background-size: 300% 300%;
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				color: transparent;
				text-shadow: 
					0 0 30px var(--glow-gold),
					0 0 60px var(--glow-action),
					2px 2px 4px rgba(0, 0, 0, 0.9);
				position: relative;
				text-transform: uppercase;
				letter-spacing: 3px;
				line-height: 1.1;
				background-position: 0% 50%;
				animation: title-shift 8s ease-in-out infinite;
			}

			@keyframes title-shift {
				0%, 100% { background-position: 0% 50%; }
				50% { background-position: 100% 50%; }
			}

			.title-epic::after {
				content: attr(data-text);
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(135deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				z-index: -1;
				filter: blur(15px);
				opacity: 0.6;
				background-size: 300% 300%;
				animation: title-shift 8s ease-in-out infinite reverse;
			}

			@media (min-width: 768px) {
				.title-epic {
					font-size: 6rem;
				}
			}

			/* Navigation améliorée - plus compacte */
			.nav-epic {
				background: linear-gradient(135deg, rgba(20, 20, 20, 0.95), rgba(40, 40, 40, 0.95));
				backdrop-filter: blur(20px);
				border-bottom: 2px solid var(--accent-royal);
				box-shadow: 
					0 4px 20px rgba(0, 0, 0, 0.8),
					inset 0 1px 0 rgba(139, 115, 85, 0.3);
				padding: 0.75rem 0;
				position: relative;
				z-index: 1000;
			}

			.nav-epic::before {
				content: '';
				position: absolute;
				bottom: -2px;
				left: 0;
				right: 0;
				height: 2px;
				background: linear-gradient(90deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				animation: border-flow 3s linear infinite;
			}

			/* Liens de navigation améliorés - plus petits */
			.nav-link-epic {
				position: relative;
				color: #E8E0D5;
				font-family: var(--font-gothic);
				text-transform: uppercase;
				letter-spacing: 0.5px;
				font-weight: 600;
				padding: 0.5rem 1rem;
				transition: all 0.4s ease;
				text-decoration: none;
				font-size: 0.9rem;
				text-shadow: 0 2px 4px rgba(0, 0, 0, 0.8);
				border-radius: 4px;
				transition: all 0.4s ease;
				transform: translateX(-50%);
			}

			.nav-link-epic:hover {
				color: var(--accent-royal);
				text-shadow: 0 0 10px var(--glow-gold);
				transform: translateY(-2px);
			}

			.nav-link-epic:hover::before {
				width: 100%;
			}

			/* Footer épique */
			.footer-epic {
				background: linear-gradient(135deg, var(--bg-dominant), var(--bg-secondary));
				border-top: 3px solid transparent;
				position: relative;
				overflow: hidden;
				padding: 4rem 0;
			}

			.footer-epic::before {
				content: '';
				position: absolute;
				top: -3px;
				left: 0;
				right: 0;
				height: 3px;
				background: linear-gradient(90deg, var(--accent-royal), var(--accent-action), var(--accent-magical), var(--accent-royal));
				animation: border-flow 4s linear infinite;
			}

			/* Effet de grille magique */
			.magic-grid {
				background-image: 
					linear-gradient(rgba(212, 175, 55, 0.1) 1px, transparent 1px),
					linear-gradient(90deg, rgba(212, 175, 55, 0.1) 1px, transparent 1px);
				background-size: 50px 50px;
				background-position: -1px -1px;
				animation: grid-shift 10s linear infinite;
			}

			@keyframes grid-shift {
				0% { background-position: -1px -1px; }
				100% { background-position: 49px 49px; }
			}

			/* Badge épique */
			.badge-epic {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				padding: 0.5rem 1rem;
				background: linear-gradient(135deg, var(--accent-action), var(--accent-magical));
				color: var(--text-primary);
				border-radius: 2rem;
				font-weight: 600;
				font-size: 0.875rem;
				text-transform: uppercase;
				letter-spacing: 1px;
				box-shadow: 0 4px 15px rgba(114, 47, 55, 0.4);
				animation: pulse 2s ease-in-out infinite;
			}

			/* Section hero améliorée */
			.hero-section {
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				position: relative;
				overflow: hidden;
				background: linear-gradient(135deg, var(--bg-dominant), var(--bg-secondary));
			}

			.hero-section::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: 
					radial-gradient(circle at 20% 20%, rgba(212, 175, 55, 0.2) 0%, transparent 50%),
					radial-gradient(circle at 80% 80%, rgba(114, 47, 55, 0.2) 0%, transparent 50%),
					radial-gradient(circle at 50% 50%, rgba(75, 0, 130, 0.1) 0%, transparent 70%);
				animation: ethereal-float 10s ease-in-out infinite;
			}

			/* Effet de curseur magique */
			.magical-cursor {
				cursor: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32"><circle cx="16" cy="16" r="8" fill="%23D4AF37" opacity="0.8"/><circle cx="16" cy="16" r="4" fill="%23722F37"/></svg>') 16 16, auto;
			}

			/* Effet de scroll magique */
			.magical-scroll {
				scrollbar-width: thin;
				scrollbar-color: var(--accent-royal) var(--bg-dominant);
			}

			.magical-scroll::-webkit-scrollbar {
				width: 8px;
			}

			.magical-scroll::-webkit-scrollbar-track {
				background: var(--bg-dominant);
			}

			.magical-scroll::-webkit-scrollbar-thumb {
				background: linear-gradient(var(--accent-royal), var(--accent-action));
				border-radius: 4px;
			}

			/* Effet de chargement magique */
			.magical-loading {
				display: inline-block;
				width: 20px;
				height: 20px;
				border: 3px solid var(--border-gold);
				border-top: 3px solid var(--accent-royal);
				border-radius: 50%;
				animation: mystical-rotate 1s linear infinite;
			}

			/* Effet de hover magique pour les liens */
			.magical-link {
				position: relative;
				text-decoration: none;
				transition: all 0.3s ease;
			}

			.magical-link::before {
				content: '';
				position: absolute;
				bottom: -2px;
				left: 0;
				width: 0;
				height: 2px;
				background: linear-gradient(90deg, var(--accent-royal), var(--accent-action), var(--accent-magical));
				transition: width 0.4s ease;
			}

			.magical-link:hover::before {
				width: 100%;
			}

			.magical-link:hover {
				color: var(--accent-royal);
				text-shadow: 0 0 10px var(--glow-gold);
			}

			.hidden {
				display: none;
			}

			.block {
				display: block;
			}

			@media (min-width: 768px) {
				.md\:block {
					display: block;
				}
			}

			.flex-col {
				flex-direction: column;
			}

			.flex-row {
				flex-direction: row;
			}

			@media (min-width: 640px) {
				.sm\:flex-row {
					flex-direction: row;
				}
			}

			.resize-none {
				resize: none;
			}

			.focus\:outline-none:focus {
				outline: 2px solid transparent;
				outline-offset: 2px;
			}

			.focus\:border-gold:focus {
				border-color: #FFD700;
			}

			.transition-colors {
				transition-property: color, background-color, border-color, text-decoration-color, fill, stroke;
				transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
				transition-duration: 150ms;
			}

			.hover\:text-gold:hover {
				color: #FFD700;
			}

			.hover\:text-parchment:hover {
				color: #F4E8D0;
			}

			.cursor-pointer {
				cursor: pointer;
			}

			.select-none {
				user-select: none;
			}

			.pointer-events-none {
				pointer-events: none;
			}

			.shadow-lg {
				box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
			}

			.shadow-2xl {
				box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
			}

			.-translate-x-1\/2 {
				--tw-translate-x: -50%;
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.w-8 {
				width: 2rem;
			}

			.h-8 {
				height: 2rem;
			}

			.text-gold\/60 {
				color: rgba(255, 215, 0, 0.6);
			}

			.fill-none {
				fill: none;
			}

			.stroke-current {
				stroke: currentColor;
			}

			.stroke-2 {
				stroke-width: 2;
			}

			.stroke-linecap-round {
				stroke-linecap: round;
			}

			.stroke-linejoin-round {
				stroke-linejoin: round;
			}

			.viewbox-0-0-24-24 {
				viewBox: 0 0 24 24;
			}

			.fill-current {
				fill: currentColor;
			}

			.overflow-hidden {
				overflow: hidden;
			}

			.relative {
				position: relative;
			}

			.pt-16 {
				padding-top: 4rem;
			}

			.border-t {
				border-top-width: 1px;
			}

			.items-center {
				align-items: center;
			}

			.justify-center {
				justify-content: center;
			}

			.text-center {
				text-align: center;
			}

			.rows-4 {
				grid-template-rows: repeat(4, minmax(0, 1fr));
			}

			.bg-green-900\/30 {
				background-color: rgba(20, 83, 45, 0.3);
			}

			.border-green-500\/30 {
				border-color: rgba(34, 197, 94, 0.3);
			}

			.text-green-400 {
				color: #4ade80;
			}

			.bg-transparent {
				background-color: transparent;
			}

			.mt-4 {
				margin-top: 1rem;
			}

			.bg-green-600 {
				background-color: #16a34a;
			}

			.disabled\:pointer-events-none:disabled {
				pointer-events: none;
			}

			.disabled\:opacity-50:disabled {
				opacity: 0.5;
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}

			.transform {
				transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1));
			}
		</style>
	</head>
	<body class="min-h-screen magical-scroll">
		<!-- Particules Magiques Flottantes -->
		<div class="particles-container">
			<div class="particle sword">⚔️</div>
			<div class="particle shield">🛡️</div>
			<div class="particle fire">🔥</div>
			<div class="particle dragon">🐉</div>
		</div>
		
		<!-- Auréole Magique Suivant le Curseur -->
		<div class="magical-aura" id="magicalAura" style="position: fixed; width: 40px; height: 40px; border-radius: 50%; background: radial-gradient(circle, rgba(212, 175, 55, 0.3), transparent); pointer-events: none; z-index: 9999; transform: translate(-50%, -50%);"></div>
		
		<!-- Étoiles Filantes -->
		<div class="shooting-stars" id="shootingStars"></div>
		
		<!-- Navigation Épique -->
		<header class="fixed top-0 w-full z-50 nav-epic">
			<nav class="container mx-auto px-4 py-2">
				<div class="flex justify-between items-center">
					<div class="flex items-center space-x-3">
						<div class="w-10 h-10 rounded-full flex items-center justify-center animate-glow magical-particles animate-dragon-roar" style="background: linear-gradient(135deg, var(--accent-action), var(--accent-magical)); border: 2px solid var(--accent-royal);">
							<svg class="icon-dragon animate-dragon-roar" viewBox="0 0 24 24" style="width: 18px; height: 18px;">
								<path d="M12,2C13.1,2 14,2.9 14,4C14,4.74 13.6,5.39 13,5.73V7H14C17.31,7 20,9.69 20,13C20,16.31 17.31,19 14,19H13V20C13,21.1 12.1,22 11,22C9.9,22 9,21.1 9,20V19H8C4.69,19 2,16.31 2,13C2,9.69 4.69,7 8,7H9V5.73C8.4,5.39 8,4.74 8,4C8,2.9 8.9,2 10,2H12M12,4H10V7H8C5.79,7 4,8.79 4,11C4,13.21 5.79,15 8,15H16C18.21,15 20,13.21 20,11C20,8.79 18.21,7 16,7H14V4H12M8,9H16C17.1,9 18,9.9 18,11C18,12.1 17.1,13 16,13H8C6.9,13 6,12.1 6,11C6,9.9 6.9,9 8,9Z"/>
							</svg>
						</div>
						<div>
							<h1 class="text-lg font-bold title-epic animate-text-glow" data-text="ROYAUMES OUBLIÉS" style="font-size: 1.1rem;">ROYAUMES OUBLIÉS</h1>
							<div class="flex items-center gap-2 mt-0">
								<svg class="icon-crown animate-crown-shine" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
									<path d="M5,16L3,5L8.5,10L12,4L15.5,10L21,5L19,16H5M19,19C19,20.1 18.1,21 17,21H7C5.9,21 5,20.1 5,19V18H19V19Z"/>
								</svg>
								<p class="text-xs animate-pulse" style="color: var(--text-secondary);">L'Héritage des Dragons</p>
							</div>
						</div>
					</div>
					
					<!-- Desktop Navigation -->
					<div class="hidden md:flex items-center space-x-6">
						<a href="#accueil" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-scroll animate-scroll-unroll" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
							</svg>
							Accueil
						</a>
						<a href="#prophetie" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-crystal animate-crystal-resonate" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M12,2L14.09,8.26L21,9L16,14L17.45,21L12,17.77L6.55,21L8,14L3,9L9.91,8.26L12,2Z"/>
							</svg>
							Prophétie
						</a>
						<a href="#royaumes" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-crown animate-crown-shine" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M5,16L3,5L8.5,10L12,4L15.5,10L21,5L19,16H5M19,19C19,20.1 18.1,21 17,21H7C5.9,21 5,20.1 5,19V18H19V19Z"/>
							</svg>
							Royaumes
						</a>
						<a href="#chroniques" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-scroll animate-scroll-unroll" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
							</svg>
							Chroniques
						</a>
						<a href="#heros" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-sword animate-sword-swing" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M6.92,5L5,7L8.09,10.09L3.5,14.59L4.91,16L9.5,11.41L13.09,15L15,13.09L11.91,10L16,5.92L14.59,4.5L10.5,8.59L6.92,5M20.41,4.5L14.5,10.41L15.91,11.82L21.82,5.91L20.41,4.5Z"/>
							</svg>
							Héros
						</a>
						<a href="#contact" class="nav-link-epic flex items-center gap-1">
							<svg class="icon-shield animate-shield-bash" viewBox="0 0 24 24" style="width: 14px; height: 14px;">
								<path d="M12,1L3,5V11C3,16.55 6.84,21.74 12,23C17.16,21.74 21,16.55 21,11V5L12,1M12,7C13.4,7 14.8,8.6 14.8,10V11.5C14.8,12.9 13.4,14.5 12,14.5C10.6,14.5 9.2,12.9 9.2,11.5V10C9.2,8.6 10.6,7 12,7Z"/>
							</svg>
							Contact
						</a>
					</div>
					
					<!-- Mobile Menu Button -->
					<button class="md:hidden magical-button animate-pulse" style="color: var(--accent-royal); border: none; background: none; cursor: pointer;" @click="$refs.mobileMenu.classList.toggle('hidden')">
						<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
						</svg>
					</button>
				</div>
				
				<!-- Mobile Menu -->
				<div x-ref="mobileMenu" class="hidden md:hidden mt-4 pt-4 border-t magical-border" style="border-color: var(--border-gold);">
					<div class="flex flex-col space-y-3">
						<a href="#accueil" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-scroll" viewBox="0 0 24 24">
								<path d="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
							</svg>
							Accueil
						</a>
						<a href="#prophetie" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-crystal" viewBox="0 0 24 24">
								<path d="M12,2L14.09,8.26L21,9L16,14L17.45,21L12,17.77L6.55,21L8,14L3,9L9.91,8.26L12,2Z"/>
							</svg>
							Prophétie
						</a>
						<a href="#royaumes" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-crown" viewBox="0 0 24 24">
								<path d="M5,16L3,5L8.5,10L12,4L15.5,10L21,5L19,16H5M19,19C19,20.1 18.1,21 17,21H7C5.9,21 5,20.1 5,19V18H19V19Z"/>
							</svg>
							Royaumes
						</a>
						<a href="#chroniques" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-scroll" viewBox="0 0 24 24">
								<path d="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
							</svg>
							Chroniques
						</a>
						<a href="#heros" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-sword" viewBox="0 0 24 24">
								<path d="M6.92,5L5,7L8.09,10.09L3.5,14.59L4.91,16L9.5,11.41L13.09,15L15,13.09L11.91,10L16,5.92L14.59,4.5L10.5,8.59L6.92,5M20.41,4.5L14.5,10.41L15.91,11.82L21.82,5.91L20.41,4.5Z"/>
							</svg>
							Héros
						</a>
						<a href="#contact" class="nav-link-epic flex items-center gap-2">
							<svg class="icon-shield" viewBox="0 0 24 24">
								<path d="M12,1L3,5V11C3,16.55 6.84,21.74 12,23C17.16,21.74 21,16.55 21,11V5L12,1M12,7C13.4,7 14.8,8.6 14.8,10V11.5C14.8,12.9 13.4,14.5 12,14.5C10.6,14.5 9.2,12.9 9.2,11.5V10C9.2,8.6 10.6,7 12,7Z"/>
							</svg>
							Contact
						</a>
					</div>
				</div>
			</nav>
		</header>
		
		<!-- Main Content -->
		<main class="pt-16">
			<slot />
		</main>
		
		<!-- Footer Épique -->
		<footer class="footer-epic">
			<div class="absolute inset-0 opacity-10">
				<div class="absolute top-10 left-10 w-32 h-32 rounded-full blur-3xl animate-ethereal-float" style="background: var(--accent-royal);"></div>
				<div class="absolute bottom-10 right-10 w-48 h-48 rounded-full blur-3xl animate-ethereal-float" style="background: var(--accent-action); animation-delay: 2s;"></div>
				<div class="absolute top-1/2 left-1/3 w-24 h-24 rounded-full blur-3xl animate-ethereal-float" style="background: var(--accent-magical); animation-delay: 4s;"></div>
			</div>
			
			<div class="container mx-auto px-4 py-16 relative z-10">
				<div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
					<div class="text-center md:text-left">
						<div class="flex items-center justify-center md:justify-start space-x-3 mb-4">
							<div class="w-12 h-12 rounded-full flex items-center justify-center magical-particles animate-glow" style="background: linear-gradient(135deg, var(--accent-action), var(--accent-magical)); border: 3px solid var(--accent-royal);">
								<svg class="icon-dragon" viewBox="0 0 24 24">
									<path d="M12,2C13.1,2 14,2.9 14,4C14,4.74 13.6,5.39 13,5.73V7H14C17.31,7 20,9.69 20,13C20,16.31 17.31,19 14,19H13V20C13,21.1 12.1,22 11,22C9.9,22 9,21.1 9,20V19H8C4.69,19 2,16.31 2,13C2,9.69 4.69,7 8,7H9V5.73C8.4,5.39 8,4.74 8,4C8,2.9 8.9,2 10,2H12M12,4H10V7H8C5.79,7 4,8.79 4,11C4,13.21 5.79,15 8,15H16C18.21,15 20,13.21 20,11C20,8.79 18.21,7 16,7H14V4H12M8,9H16C17.1,9 18,9.9 18,11C18,12.1 17.1,13 16,13H8C6.9,13 6,12.1 6,11C6,9.9 6.9,9 8,9Z"/>
								</svg>
							</div>
							<h3 class="text-2xl font-bold ancient-text animate-text-glow" style="color: var(--accent-royal); font-family: var(--font-gothic);">Royaumes Oubliés</h3>
						</div>
						<p class="text-sm mb-4 animate-fade-in" style="color: var(--text-secondary); animation-delay: 0.2s;">Plongez dans un univers où les légendes prennent vie et les dragons règnent en maîtres.</p>
						<div class="flex justify-center md:justify-start space-x-4">
							<a href="#" class="magical-link hover:text-gold transition-colors duration-300 animate-sparkle" style="color: var(--text-primary);">
								<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
									<path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
								</svg>
							</a>
							<a href="#" class="magical-link hover:text-gold transition-colors duration-300 animate-sparkle" style="color: var(--text-primary); animation-delay: 0.1s;">
								<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
									<path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/>
								</svg>
							</a>
							<a href="#" class="magical-link hover:text-gold transition-colors duration-300 animate-sparkle" style="color: var(--text-primary); animation-delay: 0.2s;">
								<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
									<path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.894 8.221l-1.97 9.28c-.145.658-.537.818-1.084.508l-3-2.21-1.446 1.394c-.14.18-.357.223-.548.223l.188-2.84 5.18-4.68c.223-.198-.054-.308-.346-.11l-6.4 4.02-2.76-.918c-.6-.187-.612-.6.125-.89l10.78-4.156c.5-.18.94.12.78.88z"/>
								</svg>
							</a>
						</div>
					</div>
					
					<div class="text-center">
						<h4 class="text-xl font-bold mb-4 ancient-text animate-text-glow" style="color: var(--accent-royal); font-family: var(--font-gothic); text-transform: uppercase; letter-spacing: 1px;">Exploration</h4>
						<ul class="space-y-2">
							<li><a href="#accueil" class="magical-link hover:text-gold transition-colors duration-300" style="color: var(--text-secondary);">Carte du Monde</a></li>
							<li><a href="#prophetie" class="magical-link hover:text-gold transition-colors duration-300" style="color: var(--text-secondary);">Anciens Reliques</a></li>
							<li><a href="#royaumes" class="magical-link hover:text-gold transition-colors duration-300" style="color: var(--text-secondary);">Bestiaire</a></li>
							<li><a href="#chroniques" class="magical-link hover:text-gold transition-colors duration-300" style="color: var(--text-secondary);">Arts Mystiques</a></li>
						</ul>
					</div>
					
					<div class="text-center md:text-right">
						<h4 class="text-xl font-bold mb-4 ancient-text animate-text-glow" style="color: var(--accent-royal); font-family: var(--font-gothic); text-transform: uppercase; letter-spacing: 1px;">Le Grimoire</h4>
						<p class="text-sm mb-4 animate-fade-in" style="color: var(--text-secondary); animation-delay: 0.3s;">Recevez les prophéties et légendes directement dans votre parchemin.</p>
						<form class="flex flex-col sm:flex-row gap-2" @submit.prevent>
							<input type="email" placeholder="votre@email.com" class="flex-1 px-4 py-2 rounded-lg magical-border animate-fade-in" style="background: rgba(26, 26, 29, 0.8); border-color: var(--border-gold); color: var(--text-primary); placeholder-color: var(--text-secondary); animation-delay: 0.4s;">
							<button type="submit" class="btn-epic animate-pulse">
								<svg class="icon-scroll" viewBox="0 0 24 24">
									<path d="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
								</svg>
								S'inscrire
							</button>
						</form>
					</div>
				</div>
				
				<div class="divider-celtic animate-shimmer"></div>
				
				<div class="text-center mt-8">
					<div class="badge-epic mb-4">
						<svg class="icon-dragon" viewBox="0 0 24 24">
							<path d="M12,2C13.1,2 14,2.9 14,4C14,4.74 13.6,5.39 13,5.73V7H14C17.31,7 20,9.69 20,13C20,16.31 17.31,19 14,19H13V20C13,21.1 12.1,22 11,22C9.9,22 9,21.1 9,20V19H8C4.69,19 2,16.31 2,13C2,9.69 4.69,7 8,7H9V5.73C8.4,5.39 8,4.74 8,4C8,2.9 8.9,2 10,2H12M12,4H10V7H8C5.79,7 4,8.79 4,11C4,13.21 5.79,15 8,15H16C18.21,15 20,13.21 20,11C20,8.79 18.21,7 16,7H14V4H12M8,9H16C17.1,9 18,9.9 18,11C18,12.1 17.1,13 16,13H8C6.9,13 6,12.1 6,11C6,9.9 6.9,9 8,9Z"/>
						</svg>
						<span>Épique & Légendaire</span>
					</div>
					<p class="text-sm animate-fade-in" style="color: var(--text-secondary); animation-delay: 0.5s;">&copy; 2024 Royaumes Oubliés. Tous droits réservés. | Forgé avec ⚔ et magie ✨</p>
				</div>
			</div>
		</footer>
		
		<!-- Scripts -->
		<script is:inline src="/scripts/main.js"></script>
		<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
		
		<!-- Scripts Magiques Améliorés -->
		<script>
			// Auréole magique suivant le curseur
			document.addEventListener('mousemove', (e) => {
				const aura = document.getElementById('magicalAura');
				if (aura) {
					aura.style.left = e.clientX + 'px';
					aura.style.top = e.clientY + 'px';
				}
			});

			// Étoiles filantes améliorées
			function createShootingStar() {
				const star = document.createElement('div');
				const symbols = ['⚔️', '🛡️', '🔥', '🐉', '✨', '💎', '👑'];
				star.innerHTML = symbols[Math.floor(Math.random() * symbols.length)];
				star.style.position = 'fixed';
				star.style.left = Math.random() * window.innerWidth + 'px';
				star.style.top = Math.random() * window.innerHeight * 0.5 + 'px';
				star.style.fontSize = Math.random() * 20 + 15 + 'px';
				star.style.color = ['#D4AF37', '#722F37', '#4B0082'][Math.floor(Math.random() * 3)];
				star.style.pointerEvents = 'none';
				star.style.zIndex = '9999';
				star.style.animation = 'magical-particles 3s ease-out forwards';
				star.style.filter = 'drop-shadow(0 0 10px currentColor)';
				document.body.appendChild(star);
				
				setTimeout(() => {
					star.remove();
				}, 3000);
			}

			// Créer des étoiles filantes périodiquement
			setInterval(createShootingStar, 3000);

			// Particules magiques au clic améliorées
			document.addEventListener('click', (e) => {
				const symbols = ['⚔️', '🛡️', '🔥', '🐉', '✨', '💎', '👑'];
				const colors = ['#D4AF37', '#722F37', '#4B0082'];
				
				for (let i = 0; i < 8; i++) {
					setTimeout(() => {
						const particle = document.createElement('div');
						particle.innerHTML = symbols[Math.floor(Math.random() * symbols.length)];
						particle.style.position = 'fixed';
						particle.style.left = e.clientX + 'px';
						particle.style.top = e.clientY + 'px';
						particle.style.fontSize = Math.random() * 20 + 10 + 'px';
						particle.style.color = colors[Math.floor(Math.random() * colors.length)];
						particle.style.pointerEvents = 'none';
						particle.style.zIndex = '9999';
						particle.style.animation = `magical-particles ${Math.random() * 2 + 1}s ease-out forwards`;
						particle.style.filter = 'drop-shadow(0 0 8px currentColor)';
						document.body.appendChild(particle);
						
						setTimeout(() => {
							particle.remove();
						}, 3000);
					}, i * 50);
				}
			});

			// Effet de parallaxe sur le scroll
			window.addEventListener('scroll', () => {
				const scrolled = window.pageYOffset;
				const parallax = document.querySelector('.particles-container');
				if (parallax) {
					parallax.style.transform = `translateY(${scrolled * 0.3}px)`;
				}
			});

			// Animation des particules flottantes
			function animateFloatingParticles() {
				const container = document.querySelector('.particles-container');
				if (!container) return;
				
				const particles = container.querySelectorAll('.particle');
				particles.forEach((particle, index) => {
					const randomX = Math.random() * window.innerWidth;
					const randomY = Math.random() * window.innerHeight;
					particle.style.left = randomX + 'px';
					particle.style.top = randomY + 'px';
					particle.style.animationDelay = (index * 2) + 's';
				});
			}

			// Initialiser les animations
			animateFloatingParticles();
			window.addEventListener('resize', animateFloatingParticles);
		</script>
	</body>
</html>
